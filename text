Hej Joshua

Ah ja, jeg tror godt jeg kan se problemet.

Du definerer in addComment funktion med én parameter — "comment". Men du kalder den fra din button onClick-handler med to argumenter, "_id" og "comment". Funktionen opfatter kun første argument, som er "_id", så hvis den værdi er undefined, så bliver selve dit comment-argument også undefined. Og det er i øvrigt unødvendigt at sende et id med i kaldet til funktionen, for det trækker du fra Quote props direkte der hvor du defineret addComment funktionen.

Derudover behøver du faktisk slet ikke sende et id med i din request body, for du har det som en parameter i din route ovre i din PUT-handler. Så du kan rette første linje til:

const quote = await Quote.findByIdAndUpdate(req.params.id, {$push: { comments: req.body.comments }});

Også fordi findByIdAndUpdate faktisk bare skal/kan tage en streng som første argument, som er selve id'et:

https://mongoosejs.com/docs/api.html#model_Model.findByIdAndUpdate

Jeg håber det giver mening? 😬 Det er altid svært at forklare på skrift.

- Dan